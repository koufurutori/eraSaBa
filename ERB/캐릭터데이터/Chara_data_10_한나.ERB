@CHARAMOVE_DATA_10(ARG, ARG:2)
LOCAL = 10
SELECTCASE ARG
	;연동설정 (RETURN=의존하는 캐릭터, RESULT:1~3=대등하게 연동되는 캐릭터)
	CASE 1
		;3인조 + 메루루는 같이 돌아다니는 편
		RESULT:1 = [[에마]]
		RESULT:2 = [[셰리]]
		RESULT:3 = [[메루루]]
	;특수생활시간 설정 (CFLAG:LOCAL:생활시간보정 을 설정)
	CASE 3
	;이동경향 (±~10=가기 쉽다/가기 어렵다, -100=가지 않는다)
	CASE 4
		SELECTCASE ARG:2
			;한나는 안뜰 자주 간다는 느낌이 있다 빨래 하기 위해서라도
			;아틀리에도

			;방에 제발 그만 처박혀!!!
			CASE P1메루루방, P2코코・한나방, P3레이아・미리아방, P4노아・안안방, P5에마・히로방, P6마고・나노카방, P7셰리・아리사방, P79대마녀방
				RETURN -10
				
			;저수조는 좀 덜 갈지도.
			CASE P37저수조
				RETURN -10

			;아틀리에 - 안뜰 라인
			CASE P36안뜰, P69복도1, P73아틀리에
				RETURN 5

			;자꾸 지하 1층에 애들 처박히는 거 막기 위해서 지하 1층의 모든 구역은 좀 덜 가게 설정하겠다
			CASE P8감옥복도, P9지하1층계단, P10소각로통로, P11소각로, P12징벌방통로, P13징벌방복도, P14징벌방1, P15징벌방2, P16징벌방3, P17징벌방4, P18징벌방5, P19징벌방6, P20징벌방7, P21징벌방8
				RETURN -5

		ENDSELECT
	;출현장소
	CASE 7
		SELECTCASE ARG:2
		;	CASE 벚꽃길토리이, 신사경내, 신사본당, 신사툇마루
		;		RETURN 1
		ENDSELECT
	;인력설정 (CFLAG:LOCAL:인력점을 설정한다)
	CASE 8
		;감옥 가 있어야 하는 시간에는 감옥에 있음
		;본래 가야하는 시간의 약 20분 전부터 움직이게 해야 그나마 들어가는 듯...
		;감옥 시간 : 0시 ~ 6시 / 10시 ~ 12시 / 15시 ~ 17시 / 22시 ~ 24시
		IF INRANGE(TIME, 0, 360) || INRANGE(TIME, 580, 720) || INRANGE(TIME, 880, 1020) || INRANGE(TIME, 1300, 1440)
			TCVAR:LOCAL:인력점 = P2코코・한나방
			;근데 못 들어갔을 때를 대비해야 하니까
			;못 들어가면 인력점을 리셋하고 배회하도록... 안 그러면 무한루프 빠지네.
			;SIF (INRANGE(TIME, 0, 360) || INRANGE(TIME, 600, 720) || INRANGE(TIME, 900, 1020) || INRANGE(TIME, 1320, 1440)) && (CFLAG:10:현재위치 != P2코코・한나방)
			;	TCVAR:LOCAL:인력점 = 0
		ENDIF

		;06:00~09:00 , 12:00~14:00, 17:00~21:00 에만 "맛없는식사" 제공
		;에마 파티는 7시-8시 / 12시 30분 - 13시 30분 / 18시 - 19시 식사
		IF INRANGE(TIME, 420, 480) || INRANGE(TIME, 750, 810) || INRANGE(TIME, 1080, 1140)
			TCVAR:LOCAL:인력점 = P45식당
		ENDIF

		;17:00~21:00에만 샤워실 사용 가능
		;샤워실은 랜덤 함수 돌리게 할까... 기본적으로 본인의 저녁시간은 피하도록.
		SELECTCASE CFLAG:LOCAL:오늘의운세 % 6
			CASE 0
				;17-17.5
				SIF INRANGE(TIME, 1021, 1050)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 1
				;17.5 - 18
				SIF INRANGE(TIME, 1050, 1080)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 2
				;19 - 19.5
				SIF INRANGE(TIME, 1140, 1170)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 3
				;19.5- 20
				SIF INRANGE(TIME, 1170, 1200)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 4
				;20 - 20.5
				SIF INRANGE(TIME, 1200, 1230)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 5
				;20.5- 21
				SIF INRANGE(TIME, 1230, 1260)
					TCVAR:LOCAL:인력점 = P35샤워실
		ENDSELECT
ENDSELECT

@EXIST_캐릭터데이터10
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터데이터10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "통칭"
	CALLF MAKE_STR(V_NAME, "한나")

;파자마 설정을 해줘야 하나... 얘들 걍 감옥 지급 옷 입고 자는데
CASE "오늘의 파자마"
	CALLF MAKE_STR(V_NAME, "한나의 파자마")


CASE "평상복：모자"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("모자", "프릴 카츄샤"))	
CASE "평상복：원피스"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("원피스", "꽃봉우리 프릴 원피스"))
CASE "평상복：신발"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("신발", "구두"))
CASE "평상복：양말"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("양말", "양말"))


;CASE "평상복：치마"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("치마", "하늘색 체크 치마"))
;CASE "평상복：액세서리"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("액세서리", "검은 마스크"))
;CASE "평상복：외투"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("외투", "케이프 망토"))
;CASE "평상복：상반신윗도리2"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신윗도리2", "조끼 결합 블라우스"))
;CASE "평상복：완부의복"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("완부의복", "붕대"))
;CASE "평상복：바지"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("바지", "스키니 팬츠"))
;CASE "평상복：그외2"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("그외2", "견장"))


CASE "고유커맨드"
	CALLF MAKE_STR(V_NAME, "우산의수리")
CASE "고유커맨드 실행 가능"
	CALLF MAKE_INT(V_NAME, (FLAG:우산내구도 > 5000 || ITEM:망가진우산) && !GETBIT(TFLAG:일일일회, 13))

CASE "고유 속옷 형용사"
	CALLF MAKE_STR(V_NAME, "작은 꽃 장식과 함께 프릴이 가득한")
CASE "고유 하반신속옷명"
	CALLF MAKE_STR(V_NAME, "프릴 장식 팬티")
CASE "고유 상반신속옷"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 상반신속옷명"
	CALLF MAKE_STR(V_NAME, "깃털 브래지어")
CASE "고유 속옷 착용 조건"
	SIF !RAND:(1 + (2 * 승부속옷판정([[한나]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)



;요리 관련
;한나는 검소한 맛을 좋아하고 부티나는 맛은 싫어했으면
CASE "요리：좋아하는 맛"
	CALLF MAKE_STR(V_NAME, "가정적/쌀/검소/건강한/새고기/우유/" + "기능적/가벼운/" * 3)
;	CALLF MAKE_STR(V_NAME, "쌀/양식/이파리/가벼운/담백/검소/품위있는/가정적/푹신푹신/" + "단/시큼한/" * 2 + "봄철/센베이/" * 3)
CASE "요리：싫어하는 맛"
	CALLF MAKE_STR(V_NAME, "정키/푸짐/스파이스/그로테스크/안주/" + "품위있는/예술적/벌레고기/호화/" * 5)
;	CALLF MAKE_STR(V_NAME, "매운/요력/" * 2 + "그로테스크/수수께끼 요리/" * 5)
CASE "요리：조수 효과"
	CALLF MAKE_STR(V_NAME, "빈곤식/특기요리＊일식＊/")
;	CALLF MAKE_STR(V_NAME, "담백한 맛/특기요리＊일식＊/")


;일단 선물 선호 관련은 차후로 미루는 것으로.

;선물 관련
;CASE "감성：좋아함" ;JP판 수정요망, 존재하지 않는 형용사 蓮華蝶の. 연꽃나비(蓮花蝶の) 오타로 추정
;	CALLF MAKE_STR(V_NAME, "빨강빛/질서/귀여운/일본풍/" + "신령/주술/부적/여우의/봄의/영원한/" * 3 + "蓮華蝶の/벛꽃의/무녀의/낙원의/봉마의/바늘/불제봉/" * 5)
;CASE "감성：싫어함"
;	CALLF MAKE_STR(V_NAME, "혼돈/서양풍/로리타/매니악/" + "손목호구/기계/" * 3 + "요괴/" * 5 + "모리야의/요괴의/" * 10)



;여기는 의뢰 ID가 관련하지 않는 정보
;의뢰 발생율 스카2/일반3/고유 5
CASE "의뢰 없음 발생율"
	CALLF MAKE_INT(V_NAME, 3)
CASE "일반의뢰발생율"
	CALLF MAKE_INT(V_NAME, 2)
CASE "고유의뢰발생율"
	CALLF MAKE_INT(V_NAME, 5)
CASE "고유의뢰최종 요소"
	CALLF MAKE_INT(V_NAME, 103)
CASEELSE
	;고유의뢰관련의 데이터에 바톤 터치

;고유 카운터 테스트

ENDSELECT

;-------------------------------------------------
;고유요리설정
;DISH_NAME          요리명
;RESULTS            요리의 종류(경식/주식/디저트）
;CALL 재료설정("")  재료의 설정。"범용"을 넣으면 자동으로 범용 식재료가 된다
;DISH_TASTE         맛의 설정
;DISH_COMMENT       (임의）한줄 코멘트
;DISH_TIME          (임의）조리 소요 시간
;DISH_MAXLIMIT      (임의）요리의 지속 시간
;-------------------------------------------------
@SPECIAL_MENU_10
DISH_NAME = 만세탕
RESULTS = 주식
CALL 재료설정("개구리")
DISH_TASTE = 중화/국물/야취/야취/검소/가벼운/따뜻한/그로테스크/
DISH_COMMENT = 잘못 요리하면 흙맛이 나는 개구리를 사용한 요리. 개구리를 산 채로 끓는 물에 집어넣어 만든다. 쟤료를 비교적 쉽게 구할 수 있어 누군가가 자주 먹었던 음식인 듯 하다.
RETURN 1

