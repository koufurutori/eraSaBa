@CHARAMOVE_DATA_2(ARG, ARG:2)
LOCAL = 2
SELECTCASE ARG
	;연동설정 (RETURN=의존하는 캐릭터, RESULT:1~3=대등하게 연동되는 캐릭터)
	CASE 1
		;어쩔까... 노아랑 미리아랑 같이 다니게 해?
		RESULT:1 = [[노아]]
		RESULT:2 = [[미리아]]
	;특수생활시간 설정 (CFLAG:LOCAL:생활시간보정 을 설정)
	CASE 3
	;이동경향 (±~10=가기 쉽다/가기 어렵다, -100=가지 않는다)
	CASE 4
		SELECTCASE ARG:2
			;안안은 좀 오락실에 쳐박히고 안 움직이는 경향이 있지

			;방에 제발 그만 처박혀!!!
			CASE P1메루루방, P2코코・한나방, P3레이아・미리아방, P4노아・안안방, P5에마・히로방, P6마고・나노카방, P7셰리・아리사방, P79대마녀방
				RETURN -10

			;밖? 싫다! 이 몸은!
			CASE P22창고가는길, P23벽가는길, P24호수가는길, P25꽃밭가는길, P26감옥저택앞, P27게스트하우스가는길, P53호수앞, P54호숫가, P55호수, P56벽앞, P57꽃밭앞, P58꽃밭
				RETURN -5

			;저수조는 좀 덜 갈지도.
			CASE P37저수조
				RETURN -10

			;오락실이랑 아틀리에는 좋아한다.
			CASE P71오락실, P73아틀리에
				RETURN 5

			;자꾸 지하 1층에 애들 처박히는 거 막기 위해서 지하 1층의 모든 구역은 좀 덜 가게 설정하겠다
			CASE P8감옥복도, P9지하1층계단, P10소각로통로, P11소각로, P12징벌방통로, P13징벌방복도, P14징벌방1, P15징벌방2, P16징벌방3, P17징벌방4, P18징벌방5, P19징벌방6, P20징벌방7, P21징벌방8
				RETURN -5

		ENDSELECT
	;출현장소
	CASE 7
		SELECTCASE ARG:2
		;	CASE 벚꽃길토리이, 신사경내, 신사본당, 신사툇마루
		;		RETURN 1
		ENDSELECT
	;인력설정 (CFLAG:LOCAL:인력점을 설정한다)
	CASE 8
		;감옥 가 있어야 하는 시간에는 감옥에 있음
		;본래 가야하는 시간의 약 20분 전부터 움직이게 해야 그나마 들어가는 듯...
		;감옥 시간 : 0시 ~ 6시 / 10시 ~ 12시 / 15시 ~ 17시 / 22시 ~ 24시
		IF INRANGE(TIME, 0, 360) || INRANGE(TIME, 580, 720) || INRANGE(TIME, 880, 1020) || INRANGE(TIME, 1300, 1440)
			TCVAR:LOCAL:인력점 = P4노아・안안방
			;근데 못 들어갔을 때를 대비해야 하니까
			;못 들어가면 인력점을 리셋하고 배회하도록... 안 그러면 무한루프 빠지네.
			;SIF (INRANGE(TIME, 0, 360) || INRANGE(TIME, 600, 720) || INRANGE(TIME, 900, 1020) || INRANGE(TIME, 1320, 1440)) && (CFLAG:2:현재위치 != P4노아・안안방)
			;	TCVAR:LOCAL:인력점 = 0
		ENDIF

		;06:00~09:00 , 12:00~14:00, 17:00~21:00 에만 "맛없는식사" 제공
		;안안 미리아 코코는 7시 30분 - 8시 30분 / 13시 - 14시 / 20시 - 21시
		;노아도 얘들이랑 같이 밥을 먹는데 33% 확률로만 그날 나옴 CFLAG:LOCAL:오늘의운세 % 3 == 1
		IF INRANGE(TIME, 450, 510) || INRANGE(TIME, 780, 840) || INRANGE(TIME, 1200, 1260)
			TCVAR:LOCAL:인력점 = P45식당
		ENDIF

		; 안 안 은 안 씻 어

		;화수목요일 18시 ~ 19시 30분까지는 미리아와 오락실에서 영화 감상
		IF YOUBI_MATCH(DAY, "화수목") && INRANGE(TIME, 1080, 1170)
			TCVAR:LOCAL:인력점 = P71오락실
		;월 금 18시 ~ 19시 30분까지는 노아와 아틀리에에서 대 문 호 의 시간
		ELSEIF FLAG:아틀리에개방 &&	INRANGE(TIME, 1080, 1170)
			TCVAR:LOCAL:인력점 = P73아틀리에
		ENDIF
ENDSELECT

@EXIST_캐릭터데이터2
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터데이터2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "통칭"
	CALLF MAKE_STR(V_NAME, "안안")

;여기서부터 의상 설정

;파자마 설정을 해줘야 하나... 얘들 걍 감옥 지급 옷 입고 자는데
CASE "오늘의 파자마"
	CALLF MAKE_STR(V_NAME, "안안의 파자마")

CASE "평상복：액세서리"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("액세서리", "스케치북"))
CASE "평상복：모자"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("모자", "프릴 헤드 드레스"))
CASE "평상복：신발"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("신발", "슬리퍼"))
CASE "평상복：양말"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("양말", "양말"))
CASE "평상복：원피스"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("원피스", "프릴 원피스"))


;CASE "평상복：바지"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("바지", "호박 바지"))
;CASE "평상복：그외1"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("그외1", "가터벨트"))
;CASE "평상복：그외2"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("그외2", "브로치"))
;CASE "평상복：치마"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("치마", "치마"))
;CASE "평상복：상반신윗도리2"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신윗도리2", "벚꽃 장식 흰 블라우스"))
;CASE "평상복：외투"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("외투", "슬릿 롱 베스트"))



;CASE "고유커맨드"
;	CALLF MAKE_STR(V_NAME, "운세뽑기계약")
;CASE "고유커맨드 실행 가능"
;	CALLF MAKE_INT(V_NAME, !FLAG:운세뽑기계약)

CASE "고유 속옷 형용사"
	CALLF MAKE_STR(V_NAME, "꿈꾸고 있는 듯한 하얀 양 장식이 앞에 달린")
CASE "고유 하반신속옷명"
	CALLF MAKE_STR(V_NAME, "양 장식 팬티")
CASE "고유 상반신속옷"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 상반신속옷명"
	CALLF MAKE_STR(V_NAME, "잠자는 양 브래지어")
CASE "고유 속옷 착용 조건"
	SIF !RAND:(1 + (2 * 승부속옷판정([[안안]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)




;요리 관련
;안안은 입맛 애새끼 같을 듯... 음식 맛을 좆되게 하는 선두주자가 되어다오
CASE "요리：좋아하는 맛"
	CALLF MAKE_STR(V_NAME, "양식/뭉근뭉근/치즈/그로테스크/정키/" + "단/예술적/" * 2 + "카레/" * 5)
;	CALLF MAKE_STR(V_NAME, "쌀/양식/이파리/가벼운/담백/검소/품위있는/가정적/푹신푹신/" + "단/시큼한/" * 2 + "봄철/센베이/" * 3)
CASE "요리：싫어하는 맛"
	CALLF MAKE_STR(V_NAME, "담백/시큼한/검소/약효/정력/콩/수수께끼 요리/" * 2 + "야채/이파리/벌레고기/" * 3)
;	CALLF MAKE_STR(V_NAME, "매운/요력/" * 2 + "그로테스크/수수께끼 요리/" * 5)
CASE "요리：조수 효과"
	CALLF MAKE_STR(V_NAME, "몰카맛/")
;	CALLF MAKE_STR(V_NAME, "담백한 맛/특기요리＊일식＊/")


;일단 선물 선호 관련은 차후로 미루는 것으로.

;선물 관련
;CASE "감성：좋아함" ;JP판 수정요망, 존재하지 않는 형용사 蓮華蝶の. 연꽃나비(蓮花蝶の) 오타로 추정
;	CALLF MAKE_STR(V_NAME, "빨강빛/질서/귀여운/일본풍/" + "신령/주술/부적/여우의/봄의/영원한/" * 3 + "蓮華蝶の/벛꽃의/무녀의/낙원의/봉마의/바늘/불제봉/" * 5)
;CASE "감성：싫어함"
;	CALLF MAKE_STR(V_NAME, "혼돈/서양풍/로리타/매니악/" + "손목호구/기계/" * 3 + "요괴/" * 5 + "모리야의/요괴의/" * 10)


;여기는 의뢰 ID가 관련하지 않는 정보
;의뢰 발생율 스카2/일반3/고유 5
CASE "의뢰 없음 발생율"
	CALLF MAKE_INT(V_NAME, 3)
CASE "일반의뢰발생율"
	CALLF MAKE_INT(V_NAME, 2)
CASE "고유의뢰발생율"
	CALLF MAKE_INT(V_NAME, 5)
CASE "고유의뢰최종 요소"
	CALLF MAKE_INT(V_NAME, 103)
CASEELSE
	;고유의뢰관련의 데이터에 바톤 터치

;고유 카운터 테스트

ENDSELECT

;-------------------------------------------------
;고유요리설정
;DISH_NAME          요리명
;RESULTS            요리의 종류(경식/주식/디저트）
;CALL 재료설정("")  재료의 설정。"범용"을 넣으면 자동으로 범용 식재료가 된다
;DISH_TASTE         맛의 설정
;DISH_COMMENT       (임의）한줄 코멘트
;DISH_TIME          (임의）조리 소요 시간
;DISH_MAXLIMIT      (임의）요리의 지속 시간
;-------------------------------------------------
@SPECIAL_MENU_2
DISH_NAME = 카레 4세트
RESULTS = 주식
CALL 재료설정("당근", "고구마", "양파", "마법의버섯")
DISH_TASTE = 양식/카레/쌀/푸짐/푸짐/푸짐/푸짐/따뜻한/뭉근뭉근/예술적/치즈/
DISH_COMMENT = 응? 카레 2세트 주문하겠다고? 그럼 이 몸도 똑같이 카레 2세트로 할까.
RETURN 1
