@CHARAMOVE_DATA_8(ARG, ARG:2)
LOCAL = 8
SELECTCASE ARG
	;연동설정 (RETURN=의존하는 캐릭터, RESULT:1~3=대등하게 연동되는 캐릭터)
	CASE 1
		;아리사도 혼자 다녀
	;특수생활시간 설정 (CFLAG:LOCAL:생활시간보정 을 설정)
	CASE 3
	;이동경향 (±~10=가기 쉽다/가기 어렵다, -100=가지 않는다)
	CASE 4
		SELECTCASE ARG:2
			;호수! 호수! 호수! 호수!
			;하지만 호수 안 자체는 잘 안 들어갈지도

			;방에 제발 그만 처박혀!!!
			CASE P1메루루방, P2코코・한나방, P3레이아・미리아방, P4노아・안안방, P5에마・히로방, P6마고・나노카방, P7셰리・아리사방, P79대마녀방
				RETURN -10

			;저수조는 좀 덜 갈지도.
			CASE P37저수조
				RETURN -10

			;호수 좋아.
			CASE P53호수앞, P54호숫가
				RETURN 10

			;안뜰도 해먹 만들 정도로 좋아하긴 해
			CASE P36안뜰
				RETURN 10

			;하지만 그 안으로는 잘 갈 생각 없어
			CASE P55호수
				RETURN -5

			;자꾸 지하 1층에 애들 처박히는 거 막기 위해서 지하 1층의 모든 구역은 좀 덜 가게 설정하겠다
			CASE P8감옥복도, P9지하1층계단, P10소각로통로, P11소각로, P12징벌방통로, P13징벌방복도, P14징벌방1, P15징벌방2, P16징벌방3, P17징벌방4, P18징벌방5, P19징벌방6, P20징벌방7, P21징벌방8
				RETURN -5

		ENDSELECT
	;출현장소
	CASE 7
		SELECTCASE ARG:2
		;	CASE 벚꽃길토리이, 신사경내, 신사본당, 신사툇마루
		;		RETURN 1
		ENDSELECT
	;인력설정 (CFLAG:LOCAL:인력점을 설정한다)
	CASE 8
		;감옥 가 있어야 하는 시간에는 감옥에 있음
		;본래 가야하는 시간의 약 20분 전부터 움직이게 해야 그나마 들어가는 듯...
		;감옥 시간 : 0시 ~ 6시 / 10시 ~ 12시 / 15시 ~ 17시 / 22시 ~ 24시
		IF INRANGE(TIME, 0, 360) || INRANGE(TIME, 580, 720) || INRANGE(TIME, 880, 1020) || INRANGE(TIME, 1300, 1440)
			TCVAR:LOCAL:인력점 = P7셰리・아리사방
			;근데 못 들어갔을 때를 대비해야 하니까
			;못 들어가면 인력점을 리셋하고 배회하도록... 안 그러면 무한루프 빠지네.
			;SIF (INRANGE(TIME, 0, 360) || INRANGE(TIME, 600, 720) || INRANGE(TIME, 900, 1020) || INRANGE(TIME, 1320, 1440)) && (CFLAG:8:현재위치 != P7셰리・아리사방)
			;	TCVAR:LOCAL:인력점 = 0
		ENDIF

		;06:00~09:00 , 12:00~14:00, 17:00~21:00 에만 "맛없는식사" 제공
		;아리사(홀로 1시간이란 느낌)
		;6-7 / 12-13 / 17-18
		IF INRANGE(TIME, 361, 420) || INRANGE(TIME, 721, 780) || INRANGE(TIME, 1021, 1080)
			TCVAR:LOCAL:인력점 = P45식당
		ENDIF

		
		;17:00~21:00에만 샤워실 사용 가능
		;샤워실은 랜덤 함수 돌리게 할까... 기본적으로 본인의 저녁시간은 피하도록.
		SELECTCASE CFLAG:LOCAL:오늘의운세 % 6
			CASE 0
				;18 - 18.5
				SIF INRANGE(TIME, 1080, 1110)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 1
				;18.5 - 19
				SIF INRANGE(TIME, 1110, 1140)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 2
				;19 - 19.5
				SIF INRANGE(TIME, 1140, 1170)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 3
				;19.5- 20
				SIF INRANGE(TIME, 1170, 1200)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 4
				;20 - 20.5
				SIF INRANGE(TIME, 1200, 1230)
					TCVAR:LOCAL:인력점 = P35샤워실
			CASE 5
				;20.5- 21
				SIF INRANGE(TIME, 1230, 1260)
					TCVAR:LOCAL:인력점 = P35샤워실
		ENDSELECT
	
ENDSELECT

@EXIST_캐릭터데이터8
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터데이터8(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "통칭"
	CALLF MAKE_STR(V_NAME, "아리사")

;파자마 설정을 해줘야 하나... 얘들 걍 감옥 지급 옷 입고 자는데
CASE "오늘의 파자마"
	CALLF MAKE_STR(V_NAME, "아리사의 파자마")

CASE "평상복：액세서리"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("액세서리", "검은 마스크"))	
CASE "평상복：바지"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("바지", "스키니 팬츠"))
CASE "평상복：신발"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("신발", "하이 부츠"))
CASE "평상복：양말"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("양말", "양말"))
CASE "평상복：외투"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("외투", "원피스형 후드"))
CASE "평상복：상반신윗도리2"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신윗도리2", "나시"))



;CASE "평상복：모자"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("모자", "검은 고글 모자"))
;CASE "평상복：원피스"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("원피스", "체크 원피스"))
;CASE "평상복：완부의복"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("완부의복", "붕대"))
;CASE "평상복：치마"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("치마", "치마"))
;CASE "평상복：그외2"
;	CALLF MAKE_INT(V_NAME, CLOTHES_ID("그외2", "견장"))



;CASE "고유커맨드"
;	CALLF MAKE_STR(V_NAME, "운세뽑기계약")
;CASE "고유커맨드 실행 가능"
;	CALLF MAKE_INT(V_NAME, !FLAG:운세뽑기계약)

CASE "고유 속옷 형용사"
	CALLF MAKE_STR(V_NAME, "붉은 포인트가 들어간 스포티한 느낌이 나는")
CASE "고유 하반신속옷명"
	CALLF MAKE_STR(V_NAME, "적흑 스포츠 팬티")
CASE "고유 상반신속옷"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 상반신속옷명"
	CALLF MAKE_STR(V_NAME, "스트랩리스 브래지어")
CASE "고유 속옷 착용 조건"
	SIF !RAND:(1 + (2 * 승부속옷판정([[아리사]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)




;요리 관련
;물고기 좋아해주면 안 될까 안 된다고? 알았어....
;매운 거...랑 단 거랑 둘 다 좋아할지도. 아리사는 꽤 모범적이고...
CASE "요리：좋아하는 맛"
	CALLF MAKE_STR(V_NAME, "일식/시큼한/매운/단/차가운/쫄깃한/스파이스/" + "말랑말랑/" * 3)
;	CALLF MAKE_STR(V_NAME, "쌀/양식/이파리/가벼운/담백/검소/품위있는/가정적/푹신푹신/" + "단/시큼한/" * 2 + "봄철/센베이/" * 3)
CASE "요리：싫어하는 맛"
	CALLF MAKE_STR(V_NAME, "푹신푹신/예술적/가정적/호쾌/짠/정키/" + "벌레고기/수수께끼 요리/" * 5)
;	CALLF MAKE_STR(V_NAME, "매운/요력/" * 2 + "그로테스크/수수께끼 요리/" * 5)
CASE "요리：조수 효과"
	CALLF MAKE_STR(V_NAME, "화학적/")
;	CALLF MAKE_STR(V_NAME, "담백한 맛/특기요리＊디저트＊/")


;일단 선물 선호 관련은 차후로 미루는 것으로.

;선물 관련
;CASE "감성：좋아함" ;JP판 수정요망, 존재하지 않는 형용사 蓮華蝶の. 연꽃나비(蓮花蝶の) 오타로 추정
;	CALLF MAKE_STR(V_NAME, "빨강빛/질서/귀여운/일본풍/" + "신령/주술/부적/여우의/봄의/영원한/" * 3 + "蓮華蝶の/벛꽃의/무녀의/낙원의/봉마의/바늘/불제봉/" * 5)
;CASE "감성：싫어함"
;	CALLF MAKE_STR(V_NAME, "혼돈/서양풍/로리타/매니악/" + "손목호구/기계/" * 3 + "요괴/" * 5 + "모리야의/요괴의/" * 10)



;여기는 의뢰 ID가 관련하지 않는 정보
;의뢰 발생율 스카2/일반3/고유 5
CASE "의뢰 없음 발생율"
	CALLF MAKE_INT(V_NAME, 3)
CASE "일반의뢰발생율"
	CALLF MAKE_INT(V_NAME, 2)
CASE "고유의뢰발생율"
	CALLF MAKE_INT(V_NAME, 5)
CASE "고유의뢰최종 요소"
	CALLF MAKE_INT(V_NAME, 103)
CASEELSE
	;고유의뢰관련의 데이터에 바톤 터치

;고유 카운터 테스트

ENDSELECT

;-------------------------------------------------
;고유요리설정
;DISH_NAME          요리명
;RESULTS            요리의 종류(경식/주식/디저트）
;CALL 재료설정("")  재료의 설정。"범용"을 넣으면 자동으로 범용 식재료가 된다
;DISH_TASTE         맛의 설정
;DISH_COMMENT       (임의）한줄 코멘트
;DISH_TIME          (임의）조리 소요 시간
;DISH_MAXLIMIT      (임의）요리의 지속 시간
;-------------------------------------------------
@SPECIAL_MENU_8
DISH_NAME = 구운 로터
RESULTS = 경식
CALL 재료설정("로터")
DISH_TASTE = 따뜻한/따뜻한/따뜻한/시큼한/가벼운/수수께끼 요리/정력/
DISH_COMMENT = 로터를 구우면 『구운 로터』가 돼!!!!!
DISH_TIME = 40
RETURN 1
