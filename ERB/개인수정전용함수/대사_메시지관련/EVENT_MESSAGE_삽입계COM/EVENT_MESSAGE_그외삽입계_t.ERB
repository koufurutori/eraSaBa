;-------------------------------------------------
;64, 역강간
;-------------------------------------------------
@MESSAGE_COM64
;역강간과 기승위 되고는 조금 뉘앙스가 다르다는 생각이 하지만, 수요가 확실할 때까지 보류합니다
;;연속
;IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
;	PRINTFORML %마스터는% %CALLNAME:TARGET%의 %TINKO(MASTER)%를 꽉 먹어 잡은 채로, 집요하게 허리를 흔들어 세웠다…
;ELSE
;	PRINTFORM %마스터는% %CALLNAME:TARGET%의 %TINKO(MASTER)%를 억지로,
;	;조교자가 처녀
;	SIF TALENT:MASTER:처녀 == 1
;		PRINT 남을 받아들인 일이 없다
;	PRINTL 바기나의 안쪽까지 질불공평응이다…
;ENDIF
;역강간은 MASTER 주도인데, 지문이 TARGET 주도가 되고 있기 때문에, 절대로 변경
[SKIPSTART]
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	PRINTFORML %타겟은% 질내로부터 뽑지 않고 %마스터를% 안아 올려 아래로부터 허리를 밀어 올렸다…
ELSE
	IF TALENT:MASTER:처녀 == 1
		;역강간은 당신 주도의 당신구멍 상대장대와 생각 수정
		;PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 걸터앉아, 천천히 앉아
		PRINTFORM %마스터는% %CALLNAME:TARGET%의 위에 걸터앉아, 천천히 앉아
		;처녀＋작은 체형
		IF TALENT:MASTER:체형 < 0
			PRINTFORML , 미개척의 어린 균열로 %TINKO(MASTER)%를 삼켜 갔다…
		;처녀
		ELSE
			PRINTFORML , 미개척의 비렬로 %TINKO(MASTER)%를 삼켜 갔다…
		ENDIF
	ELSE
		;역강간은 당신 주도의 당신구멍 상대장대와 생각 수정
		;PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 걸터앉아,
		PRINTFORM %마스터는% %CALLNAME:TARGET%의 위에 걸터앉아,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORML %SLIT()%에 %TINKO(TARGET)%를 갖다 대고 삼켜 갔다…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%에
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드에
		ENDIF
		PRINTFORML 전신을 관철해지는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF
[SKIPEND]
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	PRINTFORML %CALLNAME:TARGET%의 %TINKO(TARGET)%를, 질 구멍으로 큥큥하고 단단히 조이면서 %마스터는% 허덕이고 있다
ELSE
	IF TALENT:MASTER:처녀 == 1
		PRINTFORML %마스터는% %CALLNAME:TARGET%의 위에 걸터앉아, 스스로 %TINKO(TARGET)%를 질에 이끌어 간다
		;처녀＋작은 체형
		IF TALENT:MASTER:체형 < 0
			PRINTFORML 아직 미개척의 어린 균열을 나누어 열어, %TINKO(TARGET)%를 천천히 물었다…
		;처녀
		ELSE
			PRINTFORML 처음의 증거를 안쪽에 숨긴 비렬을 나누어 열어, %TINKO(MASTER)%를 천천히 삼켜 갔다…
		ENDIF
	ELSE
		PRINTFORM %마스터는% %타겟을% 깔아 눌러,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORML 자신의 %SLIT()%에 %TINKO(TARGET)%를 갖다 대고 삼켜 갔다…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드
		ENDIF
		PRINTFORML 를 질강으로 빨면서, %마스터는% 날카로운 열명을 올리고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;66, 기승위애널
;-------------------------------------------------
@MESSAGE_COM66
;연속
IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
	PRINTFORML %타겟은% 삽입된 채로의 물건을 단단히 조여 다시 허리를 움직이기 시작했다…
;뽑지 않고
ELSEIF TCVAR:101 == MASTER
	PRINTFORML 장내로부터 뽑지 않고 %타겟을% 안아 올려 아래로부터 허리를 밀어 올렸다…
ELSE
	PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 올라타,
	;윤활 LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 된 
	PRINTFORML 아누스로 천천히 %CALLNAME:MASTER%의 물건을 삼켜 갔다…
ENDIF
;함락이 끝난 상태
IF TALENT:연모
	PRINTFORM %타겟은%
	;작은 체형
		SIF TALENT:체형 < 0
		PRINT 작은 몸으로 열심히 
	;거유
	SIF TALENT:가슴사이즈 == 1
		PRINT 크게 가슴을 흔들면서 
	;폭유
	SIF TALENT:가슴사이즈 == 2
		PRINT 몸뚱이 채 가슴을 튀기면서 
	PRINTL 스스로 허리를 움직이고 있다…
ENDIF

;-------------------------------------------------
;71, 양구멍삽입
;-------------------------------------------------
@MESSAGE_COM71
VARSET LOCAL
LOCAL:1 = MASTER_POSE(SET_V, SET_P, 1)
LOCAL:2 = MASTER_POSE(SET_A, SET_P, 1)
IF LOCAL:1
	LOCAL = LOCAL:1
ELSEIF LOCAL:2
	LOCAL = LOCAL:2
ENDIF
;LOCAL 두 구멍삽입 물러날 수 있는 사람
;연속
IF PREVCOM == SELECTCOM
	PRINTFORML %CALLNAME:LOCAL%는 앞과 뒤를 동시에 관철해져, 폭력적인 쾌감에 넋을 잃고 허덕이고 있다…
	RETURN 0
;주인 W 꽂아
ELSEIF ((LOCAL:1 == 0 || LOCAL:2 == 0) && LOCAL == TARGET) && TALENT:MASTER:쌍창
	PRINTFORML %마스터는% 2개의 자신으로 %CALLNAME:LOCAL%의 두 구멍을 관철했다…
;주인 V삽입-TARGET A삽입
ELSEIF LOCAL == LOCAL:1
	PRINTFORML %마스터는% 연결된 채로 %CALLNAME:LOCAL%의 엉덩이를 열어, %CALLNAME:TARGET%에게 뒤의 구멍도 범하도록 재촉했다…
;주인 A삽입-조수 V삽입
ELSE
	PRINTFORML %마스터는% 아누스를 관철한 상태로 %CALLNAME:LOCAL%를 안아 다리를 열게 해 %CALLNAME:TARGET%에게 앞의 구멍도 범하도록 재촉했다…
ENDIF

[SKIPSTART]
;-------------------------------------------------
;74, 그라인드 한다
;-------------------------------------------------
@MESSAGE_COM75
IF ABL:MASTER:허리 < 3
	PRINTFORML %마스터는% 허리를 %CALLNAME:TARGET%에 꽉 누른 채로 어색한 움직임으로 좌우에 움직이기 시작했다.
ELSE
	PRINTFORML %마스터는% 허리를 %CALLNAME:TARGET%에 꽉 누른 채로 천천히 'の'자를 써내려가듯 허리를 움직였다.
ENDIF
;-------------------------------------------------
;75, 그라인드 시킨다
;-------------------------------------------------
@MESSAGE_COM75
%CALLNAME:MASTER%에 스스로 움직이도록(듯이) 명령 받았다%타겟은%, 얼굴이 새빨개져서 허리를 천천히 움직이기 시작했다.
%CALLNAME:MASTER%에 스스로 움직이도록(듯이) 명령 받았다%타겟은%, 홍조 시킨 얼굴을 피기 시작하게 한이면서 허리를 공갈협박 시작했다.
%마스터가% 말할 필요도 없이 %타겟은% 변변치않은 움직임으로 허리를 전후에 움직이기 시작하는.
%마스터가% 허리의 움직임을 그만두면(자)%타겟은% 곧바로 허리를 전후에 움직이기 시작했다.
%타겟은% 쾌락을 게으름 피우기 (위해)때문에일까, %마스터가% 명할 것도 없이 허리를 크게 흔들기 시작했다.
%마스터는% %CALLNAME:TARGET%에 움직여 주도록(듯이) 간절히원함 했다. %타겟은% 기학적인 미소를 띠면서 허리를 흔들기 시작했다.


(허리 모습중. )
%타겟은% 허리를 움직이는 것에 의해 얻을 수 있는 쾌락에 열중하고 있다.
%타겟은% 녹은 표정을 숨기는 일 없이 탐욕에 쾌락게으름 피우기 위해서(때문에) 허리를 크게 움직이고 있다.
어떻게 움직이면 자신이 제일 느끼는 곳(중)이 스치는지 이해했을 것이다, %타겟은% 움직일 때마다 홍조를 띈 얼굴을 서서히 무너뜨려 갔다.
%타겟은% 군침이나 눈물이 흘러넘치는 것도 신경쓰는 일 없이, 짐승처럼 단지 계속해서 허리를 계속 거절하고 있다.
%타겟은% 초점이 맞지 않는 눈동자로 어딘가를 응시한 채로, 기계적으로 허리를 움직이고 있다.
%타겟은% 당신의 얼굴을 보면서 허리를 움직이고 있다. 자신의 쾌락보다%마스터를% 만족시키고 싶은 것 같다.
%타겟은% 어떻게 움직이면 좋은 것인지 모른채, 당황하면서 어색한 움직임으로 허리를 털고 있다.
[SKIPEND]
;-------------------------------------------------
;79, 난쟁이오나홀
;-------------------------------------------------
@MESSAGE_COM79
IF TCVAR:100 == MASTER && TARGET == TFLAG:103
	IF TALENT:MASTER:2 & 2
		PRINTFORMW %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 %CALLNAME:TARGET%으로 페니스를 훑기 시작했다……
	;페니스밴드를 사용하고 있는 경우
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 %CALLNAME:TARGET%으로 페니스를 훑기 시작했다……
	ENDIF
	SIF !FLAG:70 && EXP:V확장경험 < 25 && CFLAG:MASTER:장난 != 2
		PRINTFORML %타겟은% 눈을 하얗게 뜨고 말의 의미가 이어지지 않는 비명 소리를 지르고 있다…
ELSE
	PRINTFORM %타겟을% 움켜쥐고, 그
	;처녀
	IF TALENT:0
		PRINT 미개척의 
	;윤활 LV4 이상
	ELSEIF PALAM:4 >= PALAMLV:4
		PRINT 흠뻑 젖어 있는 
	ELSEIF PALAM:4 <= PALAMLV:2
		PRINT 제대로 젖지 않은 
	ENDIF
	IF EXP:V확장경험 < 5
		PRINTFORML 작은 균열에 %TINKO(MASTER)%를 힘으로 쑤셔 넣었다
	ELSE
		PRINTL 작은 균열을 관철했다
	ENDIF
	IF CFLAG:MASTER:장난 == 2
		IF EXP:V확장경험 < 10
			PRINTFORM %CALLNAME:TARGET%의 비소가 쁘직쁘직하고 불쾌한 소리를 내는 것도 개의치 않고, 
		ELSEIF EXP:V확장경험 > 50
			PRINTFORM 거대한 페니스를 손쉽게 물은 
		ENDIF
	ELSE
		IF ABL:마조끼 > 3 && EXP:V확장경험 > 50
			PRINT 거대한 창에 꿰뚫리면서도 황홀한 표정으로 기뻐하는  
		ELSEIF ABL:마조끼 > 2 && EXP:V확장경험 > 25
			PRINT 입을 뻐끔뻐끔 하면서도 뺨을 상기시킨 
		ELSE
			PRINT 거대한 페니스에 내장을 압박받고 고통의 절규를 지르는 
		ENDIF
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 몸을 오나홀처럼 사용해 페니스를 훑었다……
	PRINTFORML %CALLNAME:TARGET%의 배에는 %CALLNAME:MASTER%의 페니스의 형태가 그로테스크하게 떠올라, %타겟의% 작은 몸과 현저한 대비를 이루었다……
ENDIF
;-------------------------------------------------
;92, A정상위된다
;-------------------------------------------------
@MESSAGE_COM92
;A대면입위 된다
IF (!BEDROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		PRINTFORML %TINKO(TARGET)%에 장벽을 비벼지면서 %마스터는% 허리를 앞으로 쑥 내밀었다
		PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에게, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%에게 \@ ABL:새드끼 >= 3 ? 억지로 # 상냥하게 \@ 껴안겼다
		PRINTFORM %마스터는% 한쪽 발을 %CALLNAME:TARGET%의 허리에 돌려, 
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 부드럽게 녹아 있는 애널로, 
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨물듯이 물어 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히 
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 단단히 물어 간다
		ELSE
			PRINTFORML 아직도 이물에 익숙하지 않은 애널을 관철해지는 고통에, 소리를 흘린다
		ENDIF

		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가
		ENDIF
		PRINTFORML 항구를 도려내 장벽을 희롱하는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;A정상위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		PRINTFORML %TINKO(TARGET)%에 장벽을 비벼지면서 %마스터는% 위를 향하게 눕혀졌다
		PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에게, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%에게 위를 향하게 
		IF ABL:새드끼 >= 3
			PRINTFORM 난폭하게 밀어 넘어뜨려져,  
		ELSE
			PRINTFORM 눕혀져서, 
		ENDIF
		PRINTFORML %마스터는% 다리를 자신의 머리 위로 뻗었다
		PRINTFORM 스스로 엉덩이를 여는 %CALLNAME:MASTER%의,
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹아 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨물듯이 물어 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히 
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물에 익숙하지 않은 애널을 관철해져 괴로운 울음이 새었다
		ENDIF

		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가 
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가 
		ENDIF
		PRINTFORML 장벽을 도려내 범하는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;93, A후배위된다
;-------------------------------------------------
@MESSAGE_COM93
;A배면입위 된다
IF (!INROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		PRINTFORML %CALLNAME:TARGET%에게 말해지는 대로 %마스터는% \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 뒤로 쑥 내민 양손을 붙잡혀 # 양손을 벽에 붙여 \@, 애널을 쑤셔지는 쾌감에 허덕이고 있다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 뒤로 양손을 쑥 내밀도록 # 양손을 벽에 붙이도록 \@
		IF ABL:새드끼 >= 3
			PRINTFORM 명령받아, 
		ELSE
			PRINTFORM 부탁받아, 
		ENDIF
		PRINTFORML 순순히 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹아 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨물듯이 물어 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물에 익숙하지 않은 애널을 억지로 관철해져 %마스터는% 괴로운 듯한 소리를 냈다
		ENDIF
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;A후배위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는% 엎드린 자세로 되어, 애널을 쑤셔지는 쾌감에 허덕이고 있다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 엎드린 자세가
		IF ABL:새드끼 >= 3
			PRINTFORM 되도록 명령받아, 
		ELSE
			PRINTFORM 되달라고 부탁받아, 
		ENDIF
		PRINT
		PRINTFORML 순순히 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹아 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨물듯이 물어 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히 
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물에 익숙하지 않은 애널을 억지로 관철해져 %마스터는% 괴로운 듯한 소리를 냈다
		ENDIF
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;95, 유사삽입
;-------------------------------------------------
@MESSAGE_COM95
PRINTFORML %CALLNAME:TARGET% \@ TFLAG:193 ? 의 한쪽 발을 들어 올려 # 에 덮어씌워 \@ %SLIT(TARGET)%에 %TINKO(PLAYER)%를 대고,
IF PALAM:윤활 > PALAMLV:4
	PRINTFORML 버섯 갓 모양의 플라스틱이 3분의 1도 진입하지 않을 만큼 매우 얕은 삽입으로 질퍽거리는 질구를 질척질척 파헤쳤다…
ELSE
	PRINTFORML \@ TALENT:처녀 == 1? 꼭 닫힌 # 부드러운 \@ 음순에 번들거리는 플라스틱의 끝을 문질렀다…
ENDIF

SIF FLAG:70
	RETURN
IF TALENT:처녀 == 1 || TALENT:처녀 == -1
	IF PALAM:욕정 > PALAMLV:7
		PRINTFORML 불안한 표정과 정반대로 %CALLNAME:TARGET%의 허리는 안타까운 듯이 비틀리며, 미육은 그녀의 숨긴 소원을 대변하는것 같이
		PRINTFORML 페니스밴드에 착 달라붙은 채, 틈만 나면 안쪽으로 이끌려고 꿈틀거리고 있다…
	ELSE
		PRINTFORML %타겟은% \@ TALENT:무지? 무엇을 되고 있는지 알지 못하고 # 이대로 정말로 관철되는 건 아닐까 \@ 불안한 표정을 띄우고 있다…
	ENDIF
ELSE
	PRINTFORML %타겟은% 어중간한 자극에 안타까운 듯이 허리를 비틀고 있다…
ENDIF
;-------------------------------------------------
;130, 정상위된다
;-------------------------------------------------
@MESSAGE_COM130
;대면입위 된다
IF (!BEDROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		PRINTFORML %TINKO(TARGET)%에 질벽을 비벼지면서 %마스터는% 한쪽 발과 양팔을 %CALLNAME:TARGET%에게 휘감았다
		PRINTFORML 꽉 매달리는 %CALLNAME:MASTER%에게, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%에게 \@ ABL:새드끼 >= 3 ? 억지로 # \@ 껴안겨, %마스터는% 스스로 허리를 쑥 내밀어 %TINKO(TARGET)%에 질구를 힘주어 문지른다
		PRINTFORM \@ ABL:새드끼 >= 3 ? 히죽 # 기쁜듯이 \@ 웃은 %타겟이%, 
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
		PRINTFORM %SLIT()%에 
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 로 단숨에 관철해 왔다…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가 
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가 
		ENDIF
		PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;정상위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		PRINTFORML %TINKO(TARGET)%에 질벽을 비벼지면서 %마스터는% 위를 향하게 눕혀졌다
		PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에게, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%에게 위를 향하게 
		IF ABL:새드끼 >= 3
			PRINTFORM 난폭하게 밀어 넘어뜨려져, 
		ELSE
			PRINTFORM 눕혀져서, 
		ENDIF
		PRINTFORML %마스터는% 몸이 힘을 빼 다리를 열어, 스스로의 손으로 음순을 나누어 열었다
		PRINTFORM 이쪽의 은밀한 곳을 응시하며, 군침을 삼킨 %타겟이% 
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
		PRINTFORM %SLIT()%에 
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 를 갖다 대고 관철해 왔다…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가 
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가 
		ENDIF
		PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;131, 후배위된다
;-------------------------------------------------
@MESSAGE_COM131
;배면입위 된다
IF (!INROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 쑥 내민 양손을 붙잡혀 # 양손을 벽에 붙여 \@, 질강을 후벼지는 쾌감에 허덕이고 있다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 뒤로 양손을 쑥 내밀도록 # 양손을 벽에 붙이도록 \@ 
		IF ABL:새드끼 >= 3
			PRINTFORM 명령받아, 
		ELSE
			PRINTFORM 부탁받아, 
		ENDIF
		PRINTFORML 순순히 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
		PRINTFORM %SLIT()%에 갖다 대어진 \@ TALENT:TARGET:2 & 2 ? %TINKO(TARGET)% # 페니스밴드 \@에 가장 안쪽까지 관철해졌다…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가 
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가 
		ENDIF
		PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;후배위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는% 엎드린 자세로 되어, 질강을 후벼지는 쾌감에 허덕이고 있다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 엎드린 자세로 
		IF ABL:새드끼 >= 3
			PRINTFORM 되도록 명령받아, 
		ELSE
			PRINTFORM 되달라고 부탁받아, 
		ENDIF
		PRINT
		PRINTFORML 순순히 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
		PRINTFORM %SLIT()%에 갖다 대어진 \@ TALENT:TARGET:2 & 2 ? %TINKO(TARGET)% # 페니스밴드 \@에 최심부까지 관철해졌다…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;にせちん
		ELSE
			PRINTFORM 무기질적인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;132, 대면좌위된다
;-------------------------------------------------
@MESSAGE_COM132
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	PRINTFORML %TINKO(TARGET)%는 %CALLNAME:MASTER%의 질내로부터 빠지기는 커녕, 단단히 자리를 유지하고 있다
	PRINTFORML 갑자기 %타겟이% 꼭 껴안아 와, 입술을 겹치고 혀를 휘감아 오면서 다시 허리를 흔들기 시작했다…
ELSE
	PRINTFORML %CALLNAME:MASTER%의 몸은 %CALLNAME:TARGET%에 정면에서 안아올려졌다
	SIF TEQUIP:MASTER:0 & 2
		PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 
	PRINTFORM %SLIT()%에 
	IF TALENT:TARGET:2 & 2
		PRINTFORM %TINKO(TARGET)%
	;にせちん
	ELSE
		PRINTFORM 페니스밴드
	ENDIF
	PRINTFORM 가 삽입되고
	;볼개그의 유무(예정)
	SIF !TEQUIP:MASTER:볼개그
		PRINT 입맞춤을 하면서
	PRINTL 허리에 꾸욱 압력을 주었다…
	PRINTFORML 
	;ちんちん
	IF TALENT:TARGET:2 & 2
		PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
	;にせちん
	ELSE
		PRINTFORM 무기질적인 페니스밴드가
	ENDIF
	PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
ENDIF

;-------------------------------------------------
;133, 후배위된다
;-------------------------------------------------
@MESSAGE_COM133
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	PRINTFORML %TINKO(TARGET)%는 %CALLNAME:MASTER%의 질내로부터 빠지기는 커녕, 단단히 자리를 유지하고 있다
	PRINTFORML 갑자기 %CALLNAME:TARGET%의 손이 앞으로 돌아 몸을 흔들면서, 이쪽의 피부를 어루만져 왔다…
ELSE
	PRINTFORML %CALLNAME:MASTER%의 몸은 %CALLNAME:TARGET%에게 등 뒤로부터 안아올려졌다
	PRINTFORM %SLIT()%
	IF TEQUIP:MASTER:0 & 2
		PRINTFORM 에 %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 천천히 
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 가 삽입되었다…
	ELSE
		PRINTFORML 를 기다리고 있던 
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 로 천천히 관철해졌다…
	ENDIF
	PRINTFORML 
	;ちんちん
	IF TALENT:TARGET:2 & 2
		PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가 
	;にせちん
	ELSE
		PRINTFORM 무기질적인 페니스밴드가 
	ENDIF
	PRINTFORML 질벽을 젖혀 휘젓는 감각에, %마스터는% 허덕이고 있다…
ENDIF
