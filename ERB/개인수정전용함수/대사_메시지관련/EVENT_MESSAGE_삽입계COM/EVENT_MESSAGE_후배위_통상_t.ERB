
@MESSAGE_COM61통상
;배면입위
IF TFLAG:193
	;뽑지 않고
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF TEQUIP:체위2 == 2
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:체위2 == 2
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 뽑지 않고 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단 %CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 벽에 손을 붙이게 해
				PRINTFORML 뒤로부터 삽입해 다시 허리를 움직이기 시작했다…
			ENDIF
		ENDIF
	ELSE
		IF TALENT:처녀 == 1
			PRINTFORML \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 뒤에서 양손을 잡은 # 벽에 손을 붙이게 한 \@%CALLNAME:TARGET%에 엉덩이를 쑥 내밀게 했다
			PRINTFORML 미개척의 \@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@을 %TINKO(PLAYER)%로 관철했다…
		ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓았다
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올렸다
			ENDIF
			PRINTFORM %CALLNAME:TARGET%
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 에 쑥 내밀게 한 양손을 잡아 # 의 양손을 벽에 붙이게 해 \@, 뒤로부터
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 흠뻑 젖어 있는 
			;작은 체형
			IF TALENT:체형 < 0
				PRINT 어린 균열
			ELSE
				PRINT 비렬
			ENDIF
			PRINTFORML 의 가장 안쪽까지 찔러 넣었다…
		ENDIF
	ENDIF
ELSE
;뽑지 않고
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF TEQUIP:체위2 == 2
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			PRINTFORML 삽입한 채로 %타겟을% 엎드리게 해, 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		IF TALENT:처녀 == 1
			PRINTFORML 엎드리게 한 %CALLNAME:TARGET%의 엉덩이를 높이 올리게 했다
			PRINTFORML 미개척의 \@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@을 %TINKO(PLAYER)%로 관철했다…
		ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			PRINTFORM 엎드리게 한 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아, 
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려, 
			ENDIF
			PRINTFORM 뒤로부터 
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 흠뻑 젖어 있는 
			;작은 체형
			IF TALENT:체형 < 0
				PRINT 어린 균열
			ELSE
				PRINT 비렬
			ENDIF
			PRINTFORML 의 최심부까지 찔러 넣었다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;322, 상대주도후배위
;-------------------------------------------------
@MESSAGE_COM322
IF TALENT:처녀 == 1
	PRINTFORML %CALLNAME:TARGET%의 소망 대로, 그 미개척의 \@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@에 끝부분을 가져다 대며,
	PRINTFORML 꼭 다문 비순을 나누어 열어 관철해, 순결의 증거를 찢었다…
		SIF TALENT:연모
			PRINTFORML %타겟은% 아픔을 참으면서 %CALLNAME:PLAYER%를 꽉 껴안았다…
ELSE
	PRINTFORML %마스터는% %CALLNAME:TARGET%에게 덮쳐지자, 갖고 싶어하는 듯 벌렁거리는 질구에 끝부분을 천천히 가라앉혀 갔다…
	SELECTCASE ABL:TARGET:질
		CASE 0
			PRINTFORML %TINKO()%는 좁은 육도를 삐걱삐걱 비틀어 열어 밀고 나가, %CALLNAME:TARGET%의 깊숙한 곳까지 침입했다…
		CASE 1
			PRINTFORML 단단함을 남긴 질육을 나누어 열어, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 박았다…
		CASE 2,3
			PRINTFORML 주름이 꽉 들어찬 부드러운 살덩어리를 밀어 헤쳐, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 박았다…
		CASEELSE
			PRINTFORML 힘을 주지 않아도, 녹아내린 %CALLNAME:TARGET%의 미육은 기뻐하면서 %마스터를% 가장 안쪽까지 불러들였다…
	ENDSELECT
	PRINTFORML 따뜻하게 젖은 고기주름에 싸여, 걸맞은 장소에 들어간 %CALLNAME:MASTER%의 %TINKO()%는 기쁜듯이 움찔했다
ENDIF