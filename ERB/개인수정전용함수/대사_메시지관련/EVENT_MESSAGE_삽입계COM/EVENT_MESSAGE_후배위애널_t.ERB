;-------------------------------------------------
;63, 후배위애널
;-------------------------------------------------
@MESSAGE_COM63
IF CFLAG:MASTER:장난 == 2
	IF TFLAG:193
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:체위2 == 2
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 아누스에 삽입한 채로 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단 %CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 뒤로 돌아 포함
				PRINTFORML 엉덩이를 쑥 내미는 것 같은 모습을 시켜 다시 %CALLNAME:TARGET%의 아누스를 범하기 시작했다…
			ENDIF
		ELSE
			PRINTFORM 자고 있는 %타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 해
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려, 
			ENDIF
			PRINTFORM 뒤로부터 
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된  
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ELSE
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 삽입한 채로 %타겟을% 엎드리게 해, 다시 허리를 움직이기 시작했다…
		ELSE
			PRINTFORM 자고 있는 %타겟을% 엎드리게 해
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려, 
			ENDIF
			PRINTFORM 뒤로부터 
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ENDIF
ELSE
	IF TFLAG:193
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:체위2 == 2
				PRINTFORML %CALLNAME:TARGET%의 아누스에 삽입한 채로 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단 %CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  쑥 내밀게 한 양손을 잡아 # 벽에 손을 붙이게 해 \@
				PRINTFORML 뒤로부터 삽입해 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓은
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올린
			ENDIF
			PRINTFORM %CALLNAME:TARGET% 
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 에 쑥 내밀게 한 양손을 잡아 # 의 양손을 벽에 붙이게 해 \@, 뒤로부터 
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ELSE
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 삽입한 채로 %타겟을% 엎드리게 해, 다시 허리를 움직이기 시작했다…
		ELSE
			PRINTFORM 엎드리게 한 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아, 
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려, 
			ENDIF
			PRINTFORM 뒤로부터 
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;323, 상대주도후배위A
;-------------------------------------------------
@MESSAGE_COM323
PRINTFORML %마스터는% %CALLNAME:TARGET%에게 뒤에서 덮이자, \@ ABL:애널 > 2? 갖고 싶어하는 듯 벌렁거리는 # \@ 애널에 끝부분을 가져다 대며, 천천히 허리를 가라앉혀 갔다……
PRINTFORML %타겟은% 달콤한 숨을 내쉬며 삽입감을 곱씹고 있다……